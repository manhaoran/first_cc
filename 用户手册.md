# 量化策略收益回测系统 - 用户手册

## 项目结构

```
first_cc/
├── config.py              # 回测配置 (T+0/T+1, 费率, 涨跌停限制)
├── main.py                # CLI 入口
├── requirements.txt       # 依赖 (akshare, pandas, numpy, sqlalchemy)
├── data/
│   ├── database.py        # SQLite 数据库 (stock_list + daily_kline 表)
│   └── fetcher.py         # akshare 数据获取 (股票/ETF列表 + K线)
├── engine/
│   ├── broker.py          # 交易模拟器 (资金/持仓/费用/T+1交割)
│   └── backtest.py        # 回测引擎 (逐bar迭代, 绩效报告)
├── strategy/
│   ├── base.py            # 策略基类 (数据访问/指标/下单)
│   ├── loader.py          # 自定义策略加载器
│   ├── builtin/
│   │   ├── ma_cross.py    # 双均线交叉
│   │   ├── macd.py        # MACD 金叉死叉
│   │   ├── rsi.py         # RSI 超买超卖
│   │   ├── bollinger.py   # 布林带突破
│   │   └── dual_thrust.py # Dual Thrust 突破
│   └── custom/
│       └── example_strategy.py  # 示例自定义策略
```

## 快速开始

### 安装依赖

```bash
pip install -r requirements.txt
```

### 基本使用流程

```bash
# 1. 初始化数据库 (下载A股+ETF列表和历史K线数据)
python main.py init-db

# 2. 后续更新数据 (增量拉取最新交易日数据)
python main.py update-db

# 3. 查看可用策略
python main.py list-strategies

# 4. 查看数据库中的标的
python main.py list-stocks
```

### 运行回测

```bash
# 单只股票 - 双均线策略 (默认参数: fast=5, slow=20)
python main.py run -s ma_cross -c 000001

# 多只股票 - MACD策略
python main.py run -s macd -c 000001,000002,600519

# 自定义策略参数
python main.py run -s ma_cross -c 000001 -p "fast=10,slow=30"

# RSI策略自定义参数
python main.py run -s rsi -c 000001 -p "period=14,oversold=25,overbought=75"

# 布林带策略
python main.py run -s bollinger -c 000001 -p "period=20,std=2.5"

# ETF回测 (T+0模式, 无印花税)
python main.py run -s macd -c 510050 --type etf --mode T+0 --stamp-tax 0

# 指定日期范围
python main.py run -s ma_cross -c 000001 --start 2020-01-01 --end 2024-12-31

# 自定义费率和资金
python main.py run -s ma_cross -c 000001 \
    --commission 2.5 \
    --min-commission 5 \
    --stamp-tax 1 \
    --cash 200000

# 禁用涨跌停限制
python main.py run -s ma_cross -c 000001 --no-limit-check

# 导出交易记录和权益曲线
python main.py run -s ma_cross -c 000001 -o result
# 会生成: result_trades.csv 和 result_equity.csv
```

## 配置说明

### 回测配置项 (config.py 或命令行参数)

| 参数 | 命令行 | 默认值 | 说明 |
|------|--------|--------|------|
| mode | --mode | T+1 | 交易模式: T+0 或 T+1 |
| limit_up_no_buy | --no-limit-check | True | 涨停不能买入 |
| limit_down_no_sell | --no-limit-check | True | 跌停不能卖出 |
| commission_rate | --commission | 3.0 | 佣金费率 (万分之X) |
| min_commission | --min-commission | 5.0 | 最低佣金 (元) |
| stamp_tax_rate | --stamp-tax | 1.0 | 印花税 (千分之X, 仅卖出) |
| initial_cash | --cash | 100000 | 初始资金 |
| start_date | --start | None | 回测开始日期 |
| end_date | --end | None | 回测结束日期 |

## 内置策略说明

### 1. 双均线交叉 (ma_cross)
- **逻辑**: 快线上穿慢线买入，下穿卖出
- **默认参数**: fast=5, slow=20
- **适用**: 趋势跟踪

### 2. MACD (macd)
- **逻辑**: MACD柱由负转正买入，由正转负卖出
- **默认参数**: fast=12, slow=26, signal=9
- **适用**: 趋势跟踪

### 3. RSI (rsi)
- **逻辑**: RSI低于超卖线买入，高于超买线卖出
- **默认参数**: period=14, oversold=30, overbought=70
- **适用**: 震荡市

### 4. 布林带 (bollinger)
- **逻辑**: 价格触及下轨买入，触及上轨卖出
- **默认参数**: period=20, std=2.0
- **适用**: 均值回归

### 5. Dual Thrust (dual_thrust)
- **逻辑**: 价格突破上轨买入，跌破下轨卖出
- **默认参数**: lookback=20, k1=0.7, k2=0.7
- **适用**: 短线突破

## 回测结果报告

运行完成后会输出以下指标：

| 指标 | 说明 |
|------|------|
| 初始资金 | 回测开始时的可用资金 |
| 最终权益 | 回测结束时的总资产 (现金+持仓市值) |
| 累计收益率 | 整个回测期的总收益率 |
| 年化收益率 | 按252个交易日年化计算的收益率 |
| 最大回撤 | 权益曲线从峰值到谷底的最大跌幅 |
| 夏普比率 | 风险调整后收益 (无风险利率按2%) |
| 交易次数 | 完成的买卖配对次数 |
| 胜率 | 盈利交易占比 |
| 总佣金/印花税/费用 | 各项费用汇总 |

## 自定义策略开发

### 开发步骤

1. 在 `strategy/custom/` 目录下创建 `.py` 文件
2. 继承 `BaseStrategy` 类
3. 实现 `init()` 和 `next(i)` 方法
4. 运行 `python main.py list-strategies` 确认加载成功
5. 使用 `-s 文件名` 运行回测

### 基类提供的属性和方法

#### 行情数据

| 方法 | 说明 |
|------|------|
| `self.close(code, offset=0)` | 获取收盘价 |
| `self.open(code, offset=0)` | 获取开盘价 |
| `self.high(code, offset=0)` | 获取最高价 |
| `self.low(code, offset=0)` | 获取最低价 |
| `self.volume(code, offset=0)` | 获取成交量 |
| `self.pct_change(code, offset=0)` | 获取涨跌幅 |
| `self.get_bar(code, offset=0)` | 获取完整K线数据字典 |

> offset=0 为当前bar，offset=-1 为上一个bar，以此类推。

#### 技术指标

| 方法 | 说明 |
|------|------|
| `self.sma(code, period)` | 简单移动平均序列 |
| `self.ema(code, period)` | 指数移动平均序列 |
| `self.macd(code, fast, slow, signal)` | 返回 (MACD线, 信号线, 柱状图) 最新值 |
| `self.rsi(code, period=14)` | RSI 最新值 |
| `self.bollinger_bands(code, period, std)` | 返回 (中轨, 上轨, 下轨) 最新值 |
| `self.get_closes(code, lookback)` | 获取收盘价序列 |

#### 交易操作

| 方法 | 说明 |
|------|------|
| `self.buy(code, size=None, percent=None)` | 买入 (指定股数或资金比例) |
| `self.sell(code, size=None, percent=None)` | 卖出 (指定股数或持仓比例) |
| `self.sell_all(code)` | 清仓 |
| `self.position_size(code)` | 获取当前持仓股数 |
| `self.get_position(code)` | 获取持仓对象 (含均价、可用股数等) |

#### 其他属性

| 属性 | 说明 |
|------|------|
| `self.cash` | 当前可用资金 |
| `self._codes` | 回测标的代码列表 |
| `self._dates` | 回测日期列表 |
| `self._current_idx` | 当前bar索引 |
| `self._current_date` | 当前日期 |
| `self.params` | 策略参数字典 |

### 示例: 成交量过滤双均线策略

```python
from strategy.base import BaseStrategy

class MyVolumeStrategy(BaseStrategy):
    def init(self):
        self.fast = self.params.get("fast", 5)
        self.slow = self.params.get("slow", 20)
        self.vol_ratio = self.params.get("vol_ratio", 1.5)

    def next(self, i):
        if i < self.slow + 5:
            return

        for code in self._codes:
            sma_fast = self.sma(code, self.fast)
            sma_slow = self.sma(code, self.slow)

            if len(sma_fast) < 2:
                continue

            # 计算20日均量
            volumes = [self.volume(code, -j) for j in range(20)]
            volumes = [v for v in volumes if v is not None]
            if len(volumes) < 20:
                continue
            avg_vol = sum(volumes) / len(volumes)

            cur_vol = self.volume(code)
            if cur_vol is None:
                continue

            pos = self.position_size(code)

            # 金叉 + 放量买入
            if (sma_fast[-2] <= sma_slow[-2] and sma_fast[-1] > sma_slow[-1]
                    and pos == 0 and cur_vol > avg_vol * self.vol_ratio):
                self.buy(code, percent=1.0)

            # 死叉卖出
            elif sma_fast[-2] >= sma_slow[-2] and sma_fast[-1] < sma_slow[-1]:
                if pos > 0:
                    self.sell_all(code)
```

运行:

```bash
python main.py run -s my_volume_strategy -c 000001 -p "fast=5,slow=20,vol_ratio=2.0"
```

## 数据库

系统使用 SQLite 存储数据，默认路径 `data/stock_data.db`，包含两张表:

- **stock_list**: 标的列表 (code, name, type)
- **daily_kline**: 日K线数据 (code, date, open, high, low, close, volume, amount, amplitude, pct_change, change, turnover)

数据来源为 akshare，使用前复权价格。

## 注意事项

1. 首次运行 `init-db` 会下载全部A股和ETF的历史数据，耗时会比较长，建议网络良好时运行
2. 数据获取依赖 akshare 接口，如遇网络问题请重试
3. 回测默认使用前复权收盘价成交，T+1模式下当日买入次日方可卖出
4. 买入股数自动取整到100股(1手)的整数倍
5. 印花税仅在卖出时收取
6. 策略文件放在 `strategy/custom/` 目录下会自动被加载
